import { OverMouseDirective } from './over-mouse.directive';

describe('OverMouseDirective', () => {
  it('should create an instance', () => {
    const directive = new OverMouseDirective();
    expect(directive).toBeTruthy();
  });
});
